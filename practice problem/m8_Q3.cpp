#include<bits/stdc++.h>
using namespace std;
int inversion(vector<int>a){

    int mid=a.size()/2;

    vector<int>x;
    for (int i = 0; i < mid; i++)
    {
       x.push_back(a[i]);
    }

    vector<int>y;
    for (int i = mid; i < a.size(); i++)
    {
        y.push_back(a[i]);
    }

    int i,j;

    for (int i = 0; i < a.size(); i++)
    {
        
        
    }
    
    
    

}